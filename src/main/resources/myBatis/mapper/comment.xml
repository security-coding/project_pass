<?xml version="1.0" encoding="UTF-8"?>
<!-- mybatis map 선언 표시 -->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
              "http://mybatis.org/dtd/mybatis-3-mapper.dtd"> 

<mapper namespace="com.pknu.pass.comment.dao.CommentDao">
	
	
	
<!-- 	<select id="getComments" parameterType="HashMap" resultType="comment"> -->
<!-- 		select * -->
<!-- 		from (select commentContent,commentDate, mt20id -->
<!-- 		      from comments  -->
<!-- 		     	where mt20id=#{mt20id}  -->
<!-- 		     	order by commentNum desc ) human  -->
<!-- 		where rownum between 1 and #{commentRow} -->
<!-- 	</select> -->

	<select id="getComments" parameterType="HashMap" resultType="comment">
		select commentContent,commentDate, mt20id
		      from comments2 
		     	where mt20id=#{mt20id} 
		     	order by commentNum desc
	</select>
	
	<insert id="insertComment" parameterType="comment">
		insert into comments2 values(comment_seq.nextVal,
									#{commentContent},sysdate,#{mt20id})
	</insert>
	
	
	 
</mapper>
	

	
	
	
	
	
	
	
	
	
	
	
	
	
	
