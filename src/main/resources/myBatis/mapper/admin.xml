<?xml version="1.0" encoding="UTF-8"?>
<!-- mybatis map 선언 표시 -->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
              "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.pknu.pass.admin.dao.AdminDao">
	<select id="selectTotalConcert" resultType="Integer">
		SELECT COUNT(*)
		FROM CONCERT
	</select>
	
	<!-- ROWNUM Query(Paging) -->
	<select id="selectConcert" parameterType="paging" resultType="concert">
		SELECT *
		FROM ( SELECT ROWNUM RNUM, P.*
				FROM ( SELECT * 
						FROM CONCERT
						ORDER BY MT20ID) P )
		WHERE RNUM BETWEEN #{start} AND #{last}
	</select>

	<insert id="insertConcertInf" parameterType="concert">
		MERGE INTO CONCERT
				USING DUAL
				ON (mt20id = #{mt20id})
				WHEN MATCHED THEN
					UPDATE SET
						prfnm = #{prfnm},
						prfpdfrom = #{prfpdfrom},
						prfpdto = #{prfpdto},
						fcltynm = #{fcltynm},
						prfcast = #{prfcast},
						prfcrew = #{prfcrew},
						prfruntime = #{prfruntime},
						prfage = #{prfage},
						entrpsnm = #{entrpsnm},
						pcseguidance = #{pcseguidance},
						genrenm = #{genrenm},
						mt10id = #{mt10id},
						dtguidance = #{dtguidance}
				WHEN NOT MATCHED THEN
					INSERT VALUES 
					(
						#{mt20id},
						#{prfnm},
						#{prfpdfrom},
						#{prfpdto},
						#{fcltynm},
						#{prfcast},
						#{prfcrew},
						#{prfruntime},
						#{prfage},
						#{entrpsnm},
						#{pcseguidance},
						#{genrenm},
						#{mt10id},
						#{dtguidance}
					)
	</insert>
	
	<insert id="insertImageInf" parameterType="image">
			MERGE INTO IMAGE
				USING DUAL
				ON (mt20id = #{mt20id} AND imageUrl = #{imageUrl})
				WHEN MATCHED THEN
					UPDATE SET
						imageType = #{imageType},
						uploadDate = #{uploadDate}
				WHEN NOT MATCHED THEN
					INSERT VALUES
					(
						IMG_SEQ.NEXTVAL,
						#{mt20id},
						#{imageUrl},
						#{imageType},
						#{uploadDate}
					)
	</insert>

	<insert id="insertPlaceInf" parameterType="place">
		MERGE INTO PLACE
			USING DUAL
			ON (mt10id = #{mt10id})
			WHEN MATCHED THEN
				UPDATE SET
					fcltynm = #{fcltynm},
					telno = #{telno},
					relateurl = #{relateurl}
			WHEN NOT MATCHED THEN
				INSERT VALUES
				(
					#{fcltynm},
					#{mt10id},
					#{telno},
					#{relateurl},
					#{adres},
					#{sidonm},
					#{la},
					#{lo}
				)
	</insert>
	
	<insert id="insertBoxofInf" parameterType="boxof">
		MERGE INTO BOXOFFICE
			USING DUAL
			ON (cate = #{cate} AND rnum = #{rnum})
			WHEN MATCHED THEN
				UPDATE SET
					area = #{area},
					prfdtcnt = #{prfdtcnt},
					nmrs = #{nmrs}, 
					prfpd = #{prfpd},    
					prfplcnm = #{prfplcnm},
					prfnm = #{prfnm},
					mt20id = #{mt20id}
			WHEN NOT MATCHED THEN
				INSERT VALUES
				(
					#{area},     
  					#{prfdtcnt}, 
					#{nmrs},
					#{prfpd},   
					#{cate},   
					#{prfplcnm}, 
					#{prfnm},    
					#{rnum},
					#{mt20id}
				)
	</insert>
	
	<select id="getImageName" parameterType="HashMap" resultType="String">
		SELECT 
			FILENAME
		FROM 
			IMAGE 
		WHERE 
			(FILENAME = #{FILENAME} AND IMAGETYPE = #{IMGAETYPE})
	</select>
	
	<select id="imgUpdateCheck" parameterType="String" resultType="image" >
		SELECT * FROM image WHERE MT20ID = #{mt20id} and IMAGETYPE = 1
	</select>
	
	<select id="getImageList" parameterType="String" resultType="image">
		SELECT * FROM IMAGE WHERE MT20ID = #{mt20id}
	</select>
	
	<delete id="deleteImageInf" parameterType="String">
		DELETE IMAGE WHERE mt20id = #{mt20id}
	</delete>
	
	<delete id="deleteBoxofInf">
		DELETE BOXOFFICE
	</delete>
	
</mapper>
	
	
	
	
	
	
	
	
	
	
	
	
	
	
	
	
